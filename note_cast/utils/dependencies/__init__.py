

# TODO remove it
# def fetch_quote(data: NewQuoteMetadata):

#     if data.q_id is None:
#         return submit_quote(data)
#     else:
#         return QuoteQuery.read_quote_by_id(q_id=data.q_id)
